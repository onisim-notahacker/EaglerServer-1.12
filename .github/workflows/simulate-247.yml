    name: Run Periodic Task

on:
  schedule:
    # Run every 3 hours at the 0 minute mark
    - cron: '0 */3 * * *'
  workflow_dispatch:
    # Allows manual triggering from the Actions tab

jobs:
  periodic-job:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Run your desired command
      run: time set 0 
        # Replace the line below with the command you want to run periodically
        echo "Simulating 24/7 operation with a command running every 3 hours."
        # Example: node your_script.js
