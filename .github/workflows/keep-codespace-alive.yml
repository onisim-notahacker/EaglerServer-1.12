    name: Keep Codespace Alive

    on:
      schedule:
        # Runs every hour (adjust as needed for your desired frequency)
        - cron: '0 * * * *'
      workflow_dispatch: # Allows manual triggering

    jobs:
      ping-codespace:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout repository
            uses: actions/checkout@v4

          - name: Simulate activity in Codespace
            run: |
              echo "Simulating activity in Codespace at $(date)"
              # Add commands here to interact with your Codespace
              # For example, you could list files, run a simple script, etc.
              ls -la
              # You could also use a tool like 'curl' to hit a local server in your Codespace
              # if you have one running and exposed to the internet.
